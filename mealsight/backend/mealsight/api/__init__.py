"""mealsight.api — the FastAPI REST layer. mealsight.api.app.create_app()
builds the application; mealsight.api.app.app is the module-level
instance uvicorn serves directly (`uvicorn mealsight.api.app:app`).
"""
