"""MealSight seed data pipeline: ingestion and curation only.

No MCP tools, no matching logic, no agent code, no LLM calls — everything
in this package is deterministic data transformation and loading.

Run the whole pipeline with the console entry point installed by this
package: `uv run mealsight-seed` (from backend/).
"""
